{{!< default}}
<main class="site-main">

  <div class="portfolio-hero">
    <h1 class="portfolio-name">Divya Prabhakar</h1>
    <div class="portfolio-tagline">
      <p>I build experiences users trust, products compound in value, and
        user-centered design cultures that scale.
      </p>
      <p class="portfolio-role">Currently: Head of Digital Product Design at
        <a
          class="portfolio-link"
          href="https://www.formenergy.com"
          target="_blank"
          rel="noopener noreferrer"
        >Form Energy</a></p>

    </div>

  </div>

  <div class="portfolio-layout">

    <nav class="portfolio-sidebar">
      <p class="portfolio-sidebar-label">What I am...</p>
      <ul class="portfolio-nav-list">
        <li><button
            class="portfolio-nav-item active"
            data-section="spending-my-days-on"
          >Spending my days on</button></li>
        <li><button
            class="portfolio-nav-item"
            data-section="spending-my-nights-on"
          >Spending my nights on</button></li>
        <li><button
            class="portfolio-nav-item"
            data-section="observing"
          >Observing</button></li>
        <li><button
            class="portfolio-nav-item"
            data-section="curating"
          >Curating</button></li>
        <li><button
            class="portfolio-nav-item"
            data-section="organizing"
          >Organizing</button></li>
        <li><button class="portfolio-nav-item" data-section="using-ai-for">Using
            AI for</button></li>
        <li><button
            class="portfolio-nav-item"
            data-section="not-using-ai-for"
          >Not using AI for</button></li>
        <li>
          <button
            class="portfolio-nav-item portfolio-see-all"
            data-section="see-everything"
          >See everything</button>
        </li>
      </ul>

    </nav>

    <div class="portfolio-content-area">

      <div id="section-spending-my-days-on" class="portfolio-section visible">
        {{#get
          "posts"
          limit="20"
          filter="tag:spending-my-days-on"
          order="published_at desc"
          as |posts meta|
        }}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              <span class="portfolio-post-date">{{date
                  published_at
                  format="MMM YYYY"
                }}</span>
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
              <div class="portfolio-post-tags">
                {{#foreach tags}}
                  <span class="portfolio-tag-pill">{{name}}</span>
                {{/foreach}}
              </div>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

      <div id="section-spending-my-nights-on" class="portfolio-section">
        {{#get
          "posts"
          limit="20"
          filter="tag:spending-my-nights-on"
          order="published_at desc"
          as |posts meta|
        }}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              <span class="portfolio-post-date">{{date
                  published_at
                  format="MMM YYYY"
                }}</span>
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
              <div class="portfolio-post-tags">
                {{#foreach tags}}
                  <span class="portfolio-tag-pill">{{name}}</span>
                {{/foreach}}
              </div>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

      <div id="section-observing" class="portfolio-section">
        {{#get
          "posts"
          limit="20"
          filter="tag:observing"
          order="published_at desc"
          as |posts meta|
        }}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              <span class="portfolio-post-date">{{date
                  published_at
                  format="MMM YYYY"
                }}</span>
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
              <div class="portfolio-post-tags">
                {{#foreach tags}}
                  <span class="portfolio-tag-pill">{{name}}</span>
                {{/foreach}}
              </div>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

      <div id="section-curating" class="portfolio-section">
        {{#get
          "posts"
          limit="20"
          filter="tag:curating"
          order="published_at desc"
          as |posts meta|
        }}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              <span class="portfolio-post-date">{{date
                  published_at
                  format="MMM YYYY"
                }}</span>
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
              <div class="portfolio-post-tags">
                {{#foreach tags}}
                  <span class="portfolio-tag-pill">{{name}}</span>
                {{/foreach}}
              </div>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

      <div id="section-organizing" class="portfolio-section">
        {{#get
          "posts"
          limit="20"
          filter="tag:organizing"
          order="published_at desc"
          as |posts meta|
        }}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              <span class="portfolio-post-date">{{date
                  published_at
                  format="MMM YYYY"
                }}</span>
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
              <div class="portfolio-post-tags">
                {{#foreach tags}}
                  <span class="portfolio-tag-pill">{{name}}</span>
                {{/foreach}}
              </div>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

      <div id="section-using-ai-for" class="portfolio-section">
        {{#get
          "posts"
          limit="20"
          filter="tag:using-ai-for"
          order="published_at desc"
          as |posts meta|
        }}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              <span class="portfolio-post-date">{{date
                  published_at
                  format="MMM YYYY"
                }}</span>
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
              <div class="portfolio-post-tags">
                {{#foreach tags}}
                  <span class="portfolio-tag-pill">{{name}}</span>
                {{/foreach}}
              </div>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

      <div id="section-not-using-ai-for" class="portfolio-section">
        {{#get
          "posts"
          limit="20"
          filter="tag:not-using-ai-for"
          order="published_at desc"
          as |posts meta|
        }}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              <span class="portfolio-post-date">{{date
                  published_at
                  format="MMM YYYY"
                }}</span>
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
              <div class="portfolio-post-tags">
                {{#foreach tags}}
                  <span class="portfolio-tag-pill">{{name}}</span>
                {{/foreach}}
              </div>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

      <div id="section-see-everything" class="portfolio-section">
        {{#get "posts" limit="50" order="published_at desc" as |posts meta|}}
          {{#foreach posts}}
            <div class="portfolio-post-item">
              {{#primary_tag}}
                <span class="portfolio-section-label">{{name}}</span>
              {{/primary_tag}}
              <a href="{{url}}" class="portfolio-post-link">
                <h4 class="portfolio-post-title">{{title}}</h4>
              </a>
              <p class="portfolio-post-excerpt">{{excerpt words="10"}}</p>
            </div>
          {{/foreach}}
        {{/get}}
      </div>

    </div>
  </div>

</main>